function datos(){try{return JSON.parse(localStorage.getItem(DB_KEY)||"[]")}catch(e){return []}}function guardarDatos(data){localStorage.setItem(DB_KEY,JSON.stringify(data))}function agregarRegistro(reg){const data=datos();data.unshift(reg);guardarDatos(data)}function actualizarRegistro(id,patch){const data=datos();const i=data.findIndex(r=>r.id===id);if(i>=0){data[i]={...data[i],...patch};guardarDatos(data)}}function ultimoRegistro(){return datos().find(r=>!r.eliminado)||null}function registrosPor(codigo,estacion){return datos().filter(r=>!r.eliminado&&String(r.codigo).trim()===String(codigo).trim()&&r.estacion===estacion).sort((a,b)=>String(a.fecha_hora).localeCompare(String(b.fecha_hora)))}function ultimoEvento(codigo,estacion){const regs=registrosPor(codigo,estacion).filter(r=>r.evento&&r.evento!=="SIN EVENTO");return regs.length?String(regs[regs.length-1].evento).toUpperCase():""}function tieneEvento(codigo,estacion,evento){return registrosPor(codigo,estacion).some(r=>String(r.evento||"").toUpperCase()===String(evento).toUpperCase())}function ultimoEventoStock(codigo,estacion){const regs=registrosPor(codigo,estacion).filter(r=>{const ev=String(r.evento||"").toUpperCase();return ev==="EN STOCK"||ev==="SALIDA STOCK"});return regs.length?String(regs[regs.length-1].evento).toUpperCase():""}